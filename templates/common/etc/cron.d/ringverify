# By default it will send mail to the swiftops alias below
# which in turn redirects it to the proper location 
#MAILTO="swiftops"
# This cron calls the ringverify script to make sure the 
# md5 sum for the ring currently installed is proper
#5 */1 * * * root bash /usr/local/bin/ringverify.sh $RINGSERVER_IP
